apply plugin: 'java'

sourceCompatability: 1.8
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: 'libs', include: '*.jar')
}

sourceSets {
    main {
        java.srcDirs = ['src']
    }
}
